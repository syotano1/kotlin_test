# How to install kotlin
1. `$ curl -s https://get.sdkman.io | bash`を叩く
2. インストールの最後に下記のような指定のコマンドを入力しろと出るので叩く  
   `$ source "/home/[ユーザー名]/.sdkman/bin/sdkman-init.sh"`
3. `$ sdk install kotlin`を叩く
4. `$ sdk install java`を叩く